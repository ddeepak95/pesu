/**
 * Layout class strings copied from the main app for prototype parity.
 * @see PageLayout — page container (used via PageLayout component on the route)
 * @see AssessmentShell — inner assessment stack
 */

/** `AssessmentShell` root stack. */
export const APP_ASSESSMENT_SHELL_CLASS = "space-y-2 w-full";
